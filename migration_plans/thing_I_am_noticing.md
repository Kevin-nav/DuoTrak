we will need to reduce the dependencies and pacakages to only the bare miimum, 
we will need to do full and proper code cleaning to reduce the verbose code
put the frontned codes inside a proper frontend folder and then have the backend in its folder.