apiVersion: v1
kind: Service
metadata:
  name: duotrak-backend
  namespace: default
spec:
  selector:
    app: duotrak-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
