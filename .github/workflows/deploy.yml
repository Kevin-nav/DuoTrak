name: Build and Deploy to K8s

on:
  push:
    branches: ["main"]
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  FRONTEND_IMAGE: duotrak-frontend
  BACKEND_IMAGE: duotrak-backend
  K8S_NAMESPACE: default

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Normalize image owner
        id: owner
        run: echo "value=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.owner.outputs.value }}/${{ env.FRONTEND_IMAGE }}:sha-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ steps.owner.outputs.value }}/${{ env.FRONTEND_IMAGE }}:latest
          build-args: |
            NEXT_PUBLIC_API_BASE_URL=https://api.duotrak.org

      - name: Build and push backend image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          file: ./backend/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ steps.owner.outputs.value }}/${{ env.BACKEND_IMAGE }}:sha-${{ github.sha }}
            ${{ env.REGISTRY }}/${{ steps.owner.outputs.value }}/${{ env.BACKEND_IMAGE }}:latest

  deploy:
    runs-on: [self-hosted, linux, duotrak]
    needs: build-and-push
    permissions:
      actions: read
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Normalize image owner
        id: owner
        run: echo "value=${GITHUB_REPOSITORY_OWNER,,}" >> "$GITHUB_OUTPUT"

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: latest

      - name: Configure kubeconfig (optional)
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}
        run: |
          if [ -n "$KUBE_CONFIG" ]; then
            mkdir -p "$HOME/.kube"
            echo "$KUBE_CONFIG" > "$HOME/.kube/config"
            chmod 600 "$HOME/.kube/config"
            echo "Using kubeconfig from GitHub secret."
          else
            echo "KUBE_CONFIG secret not set. Using local runner kubeconfig."
          fi

      - name: Verify cluster access
        run: |
          kubectl get nodes

      - name: Apply base manifests
        run: |
          kubectl apply -f k8s/deployment.yaml -n ${{ env.K8S_NAMESPACE }}
          kubectl apply -f k8s/service.yaml -n ${{ env.K8S_NAMESPACE }}
          kubectl apply -f k8s/backend-deployment.yaml -n ${{ env.K8S_NAMESPACE }}
          kubectl apply -f k8s/backend-service.yaml -n ${{ env.K8S_NAMESPACE }}
          kubectl apply -f k8s/cloudflared-configmap.yaml -n ${{ env.K8S_NAMESPACE }}
          kubectl apply -f k8s/cloudflared-deployment.yaml -n ${{ env.K8S_NAMESPACE }}

      - name: Update frontend image
        env:
          SHA: ${{ github.sha }}
          IMAGE_OWNER_LOWER: ${{ steps.owner.outputs.value }}
        run: |
          kubectl set image deployment/duotrak-frontend \
            duotrak-frontend=${{ env.REGISTRY }}/$IMAGE_OWNER_LOWER/${{ env.FRONTEND_IMAGE }}:sha-$SHA \
            -n ${{ env.K8S_NAMESPACE }}

      - name: Update backend image
        env:
          SHA: ${{ github.sha }}
          IMAGE_OWNER_LOWER: ${{ steps.owner.outputs.value }}
        run: |
          kubectl set image deployment/duotrak-backend \
            duotrak-backend=${{ env.REGISTRY }}/$IMAGE_OWNER_LOWER/${{ env.BACKEND_IMAGE }}:sha-$SHA \
            -n ${{ env.K8S_NAMESPACE }}

      - name: Verify rollout
        run: |
          kubectl rollout status deployment/duotrak-frontend -n ${{ env.K8S_NAMESPACE }}
          kubectl rollout status deployment/duotrak-backend -n ${{ env.K8S_NAMESPACE }}

  notify:
    runs-on: ubuntu-latest
    needs: [build-and-push, deploy]
    if: always()

    steps:
      - name: Add deployment summary
        run: |
          echo "## DuoTrak Deploy Status" >> "$GITHUB_STEP_SUMMARY"
          echo "- Branch: \`${{ github.ref_name }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Commit: \`${{ github.sha }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Deploy result: \`${{ needs.deploy.result }}\`" >> "$GITHUB_STEP_SUMMARY"
          echo "- Run URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Send webhook notification
        env:
          DEPLOY_RESULT: ${{ needs.deploy.result }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          SHA: ${{ github.sha }}
          BRANCH: ${{ github.ref_name }}
          DEPLOY_WEBHOOK_URL: ${{ secrets.DEPLOY_WEBHOOK_URL }}
        run: |
          if [ -z "$DEPLOY_WEBHOOK_URL" ]; then
            echo "DEPLOY_WEBHOOK_URL not set. Skipping webhook notification."
            exit 0
          fi

          if [ "$DEPLOY_RESULT" = "success" ]; then
            STATUS_TEXT="SUCCESS"
          else
            STATUS_TEXT="FAILED"
          fi

          curl -X POST "$DEPLOY_WEBHOOK_URL" \
            -H "Content-Type: application/json" \
            -d "{\"text\":\"DuoTrak deploy ${STATUS_TEXT} on ${BRANCH} (${SHA}). Run: ${RUN_URL}\"}"
